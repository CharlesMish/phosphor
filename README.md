# Phosphor

**Draw the cycle. Draw the space. Play the result.**

Phosphor is a browser instrument built around directly manipulating DSP structures.

This repository is being prepared as the canonical playable baseline. `main` will deploy automatically to GitHub Pages once the initial source commit lands.
